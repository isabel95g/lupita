// js/login.js — ya no se usa directamente, el login está en login.html inline
// Se mantiene este archivo por compatibilidad con la estructura original del proyecto.
// La lógica de login está manejada por el script en src/views/login.html
// que llama a POST /login en el mismo servidor (puerto 3000).
